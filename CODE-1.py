def solution(nums):
    # 폰켓몬 종류 개수 계산
    count = len(set(nums))
    
    # N/2 마리를 선택하는 경우의 수 계산
    n = len(nums)
    ways = n // 2
    
    # 폰켓몬 종류별 최대 개수 계산
    max_freq = min(count, ways)
    
    return max_freq
    //해당 코드는 주어진 배열 nums에 있는 포켓몬 카드 중 최대 몇 개를 선택할 수 있는지를 계산하는 함수입니다.

먼저 set 함수를 사용하여 nums 배열에서 고유한 포켓몬 종류의 개수를 계산합니다. 중복된 값은 제거하고, 이를 count 변수에 저장합니다.
다음으로, nums 배열의 길이를 구하여 이를 n 변수에 저장합니다.
n 변수 값을 2로 나눈 몫 (즉, n//2)을 계산하여 이를 ways 변수에 저장합니다. 이는 총 가능한 선택 방법의 수를 나타냅니다.
마지막으로, count와 ways 중에서 더 작은 값인 max_freq를 계산합니다. 이 값이 최종적으로 선택할 수 있는 포켓몬 카드의 최대 개수입니다.
따라서 이 함수는 주어진 nums 배열에서 고유한 포켓몬 종류를 최대한 많이 선택하는 경우, 최대 몇 개의 카드를 선택할 수 있는지를 반환합니다.
